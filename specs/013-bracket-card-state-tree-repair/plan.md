# Plan

1. Capture the current ready, waiting, and portrait-export failures.
2. Add geometry assertions for balanced insets and absent phantom columns.
3. Add connector invariants for declared dependencies and unrelated-path
   intersections.
4. Implement state-specific match-card layouts.
5. Replace the portrait connector layout with a conventional mirrored tree.
6. Regenerate tablet and export evidence and close independent review findings.
7. Run repository, responsive, Vinext, PWA, and frontend contract gates.
